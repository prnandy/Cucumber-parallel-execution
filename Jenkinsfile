pipeline {
    agent any
   
    stages {
        stage('Example') {
            steps {
            
                withMaven(maven : 'Maven 3.6.0) {
                sh 'mvn --version'
            }
        }
    }
}